# infos.properties
#
# Property file containing global information about the project

# The project name
project.name=cerberus-robot-extension

# The project version
project.version=${project.version}

# Timestamp of the build
project.build=${timestamp}

